# AI 总入口（game 服务端）

在对话里 **`@AI-HUB.md`**（本文件）后，助手应 **`Read` 并遵循**：

**`game/.cursor/skills/project-entry/SKILL.md`**

该文件内含：要联动打开哪些 **Cursor 规则**、哪些 **子 Skill**，以及常用代码锚点；按任务类型分流后再写代码。

若 Cursor 工作区根目录是 **`share2`**（同时含 Unity 客户端），需要 **总分流** 时请先 **`@SHARE2-AI-HUB.md`**（仓库组根目录）。
